- Fix ts intersection for data when calling functions
- Form control errors and undefined stuff in clientform
- Save dashboard currency in local storage
  - Possible cache conersion rates for offline access (although the app is kinda uselss if ur offline)
- Handling offline errors
  - Displays local storage error if cannot fetch computeddata
- (maybe) refactor in the future where projects are the unit instead of clients
- use react query 
- unify all the dahsboard for the same date range
- passkeys
- database settings for realestate
